# Contributing

1. Create a feature branch.
2. Make focused changes.
3. Run the project test suite before opening a pull request.
4. Do not commit `.env`, credentials, generated reports, logs, or dependency directories.
5. For security issues, follow `SECURITY.md` instead of opening a public issue.
